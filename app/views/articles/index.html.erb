<h1>All Articles</h1>

<% @articles.each do |article| %>
<ul id="article-<%= article.id %>">
  <li><%= link_to article.title, article_path(article), class: 'article-title' %></li>
</ul>
<% end %>

<h2><%= link_to "Create New Article", new_article_path, class: 'new-article' %></h2>
