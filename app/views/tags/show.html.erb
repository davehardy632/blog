<h1><%= @tag.name %></h1>
<% @tag.articles.each do |article| %>
<p><%= link_to article.title, article_path(article) %></p>
<% end %>
<p><%= link_to "Delete Tag", tag_path(@tag), method: :delete %></p>
